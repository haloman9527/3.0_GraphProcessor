namespace GraphProcessor
{
    /// <summary> 接口方向(进/出) </summary>
    public enum PortDirection
    {
        /// <summary> 进方向 </summary>
        Input,

        /// <summary> 出方向 </summary>
        Output
    }
}
